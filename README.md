# Onilne-Bookstore
Basic react application of Project Name "Online Book Store"...

# Create React New Folder in react app

-Create two folder backend and frontend.
-then extract backend.zip and frontend.zip in the create folder and open workspace.

## ⚙️ Installation & Setup

### 1. Clone the repository
```bash
git clone https://github.com/your-username/library-management-system.git
cd library-management-system

###2. Install backend dependencies
cd Backend
npm install

npm start

###3. Install frontend dependencies
cd ../frontend
npm install

npm start

###4. Configure environment variables
Create a .env file in the Backend folder with the following:
PORT=5000
MONGO_URI=your_mongodb_connection_string
JWT_SECRET=your_jwt_secret

🧪 Testing
Use Postman to test backend API endpoints
Frontend testing with React Testing Library (optional)

